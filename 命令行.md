<!-- unset LD_LIBRARY_PATH
# 只保留：cuda12.8 + 系统基础库，删掉所有cuda13、cuda12.9、其他conda环境
export LD_LIBRARY_PATH=/usr/local/cuda-12.8/lib64:/usr/lib/x86_64-linux-gnu
export CUDA_HOME=/usr/local/cuda-12.8
SITE=$CONDA_PREFIX/lib/python3.10/site-packages
export LD_LIBRARY_PATH="$CUDA_HOME/lib64:$CONDA_PREFIX/lib:$SITE/torch/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"
echo $LD_LIBRARY_PATH -->

5s大概9分钟，20s大概半小时；
如果添加--use_dmd向视频生成命令（lyra2_zoomgs_inference或）传递参数lyra2_custom_traj_inference以启用DMD蒸馏LoRA。这会将采样步骤减少到4步并去除CFG，从而实现大约15倍的速度提升。

--use_dmd使用以下功能生成的示例输出custom_trajectory_examples/example_1（步骤 1 视频生成 | 步骤 2 GS 重建）：

生成视频
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
PYTHONPATH=. python -m lyra_2._src.inference.lyra2_zoomgs_inference \
  --input_image_path assets/samples \
  --sample_id 4 \
  --experiment lyra2 \
  --checkpoint_dir checkpoints/model \
  --prompt_dir assets/samples \
  --output_path outputs/zoomgs \
  --num_frames_zoom_in 81 \
  --num_frames_zoom_out 241 \
  --zoom_in_strength 0.5 \
  --zoom_out_strength 1.5


export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
PYTHONPATH=. python -m lyra_2._src.inference.lyra2_zoomgs_inference \
  --input_image_path assets/samples \
  --sample_id 15 \
  --experiment lyra2 \
  --checkpoint_dir checkpoints/model \
  --prompt_dir assets/samples \
  --output_path outputs/zoomgs \
  --num_frames_zoom_in 81 \
  --num_frames_zoom_out 241 \
  --zoom_in_strength 0.5 \
  --zoom_out_strength 1.5 \
  --use_dmd

选项 2：自定义轨迹
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True

PYTHONPATH=. python -m lyra_2._src.inference.lyra2_custom_traj_inference \
  --input_image_path assets/custom_trajectory_examples/example_0/first_frame.png \
  --trajectory_path assets/custom_trajectory_examples/example_0/trajectory.npz \
  --experiment lyra2 \
  --checkpoint_dir checkpoints/model \
  --captions_path assets/custom_trajectory_examples/example_0/captions.json \
  --num_frames 481 \
  --output_path outputs/custom_traj

PYTHONPATH=. python -m lyra_2._src.inference.vipe_da3_gs_recon \
  --input_video_path outputs/zoomgs/videos/<sample_id>.mp4

python -m lyra_2._src.inference.vipe_da3_gs_recon --input_video_path outputs/zoomgs/videos/04.mp4

wget https://developer.download.nvidia.com/compute/cudnn/redist/cudnn/linux-x86_64/cudnn-linux-x86_64-9.8.0.87_cuda12-archive.tar.xz

tar -xf cudnn-linux-x86_64-9.8.0.87_cuda12-archive.tar.xz
cd cudnn-linux-x86_64-9.8.0.87_cuda12-archive

# 1. 复制头文件到 cuda12.8 include
sudo cp include/cudnn*.h /usr/local/cuda-12.8/include/

# 2. 复制lib下所有cudnn库到cuda12.8 lib64，加 -P 保留软链接
sudo cp -P lib/libcudnn* /usr/local/cuda-12.8/lib64/

# 3. 赋予全局可读权限
sudo chmod a+r /usr/local/cuda-12.8/include/cudnn*.h /usr/local/cuda-12.8/lib64/libcudnn*

# 4. 更新系统动态链接缓存
sudo ldconfig

wget https://developer.download.nvidia.com/compute/redist/nccl/v2.29.2/nccl_2.29.2-1+cuda12.9_x86_64.txz

# 1.解压
tar -xf nccl_2.29.2-1+cuda12.9_x86_64.txz
cd nccl_2.29.2-1+cuda12.9_x86_64

# 2.拷贝头文件
sudo cp include/nccl*.h /usr/local/cuda-12.8/include/

# 3.拷贝库（新版只有lib，复制到cuda12.8/lib64，-P保留软链接）
sudo cp -P lib/libnccl* /usr/local/cuda-12.8/lib64/

# 4.权限+动态链接缓存
sudo chmod a+r /usr/local/cuda-12.8/include/nccl*.h /usr/local/cuda-12.8/lib64/libnccl*
sudo ldconfig



SITE=$CONDA_PREFIX/lib/python3.10/site-packages
export CPATH="$CUDA_HOME/include:$SITE/nvidia/cudnn/include:$SITE/nvidia/nccl/include:$CPATH"
export LD_LIBRARY_PATH="$CONDA_PREFIX/lib:$SITE/torch/lib:$SITE/nvidia/cuda_runtime/lib:$SITE/nvidia/cudnn/lib:$CUDA_HOME/lib64:$LD_LIBRARY_PATH"

# 头文件路径：仅系统cuda12.8（自带cudnn、nccl头文件）
export CPATH="$CUDA_HOME/include:$CPATH"

# 库路径：
# 1. CUDA12.8 lib64（cudnn+nccl+cuda库）置顶
# 2. conda自身python/torch库保留
export LD_LIBRARY_PATH="$CUDA_HOME/lib64:$CONDA_PREFIX/lib:$LD_LIBRARY_PATH"
export CC="$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-gcc"
export CXX="$CONDA_PREFIX/bin/x86_64-conda-linux-gnu-g++"


1. 备份（保存，不删除）

# 创建备份目录
mkdir -p /etc/ld.so.conf.d/bak_cuda13
# 移动13的配置文件到备份文件夹
mv /etc/ld.so.conf.d/gds-13-0.conf /etc/ld.so.conf.d/bak_cuda13/
mv /etc/ld.so.conf.d/gds-13-1.conf /etc/ld.so.conf.d/bak_cuda13/
rm -f /etc/ld.so.cache
ldconfig

现在绑定 12.8：
rm -f /usr/local/cuda
ln -s /usr/local/cuda-12.8 /usr/local/cuda

检验结果，没有出现13.0了就对了
ldconfig -p | grep libcudart


3. 后续需要恢复 CUDA13 时执行

# 把备份文件移回原目录
mv /etc/ld.so.conf.d/bak_cuda13/gds-13-*.conf /etc/ld.so.conf.d/
# 重建ld缓存
rm -f /etc/ld.so.cache
ldconfig

rm -f /usr/local/cuda
ln -s /usr/local/cuda-13.0 /usr/local/cuda

查看
env | grep -E 'CUDA|LD_LIBRARY_PATH|PATH'
ldconfig -p | grep libcudart
find /data/opt/miniconda3/envs/lyra2 -name 'libcudart.so*'
find /usr/local -name 'libcudart.so*'

# 临时备份这两个文件夹
(lyra2) root@ubuntu:/data/projects/lyra/Lyra-2# find /usr/local -name 'libcudart.so*'
/usr/local/cuda-12.8/targets/x86_64-linux/lib/libcudart.so.12
/usr/local/cuda-12.8/targets/x86_64-linux/lib/libcudart.so.12.8.90
/usr/local/cuda-12.8/targets/x86_64-linux/lib/libcudart.so
/usr/local/cuda-13.0/targets/x86_64-linux/lib/libcudart.so.13.0.96
/usr/local/cuda-13.0/targets/x86_64-linux/lib/libcudart.so.13
/usr/local/cuda-13.0/targets/x86_64-linux/lib/libcudart.so
/usr/local/cuda-12.9/targets/x86_64-linux/lib/libcudart.so.12
/usr/local/cuda-12.9/targets/x86_64-linux/lib/libcudart.so
/usr/local/cuda-12.9/targets/x86_64-linux/lib/libcudart.so.12.9.79
(lyra2) root@ubuntu:/data/projects/lyra/Lyra-2# mv /usr/local/cuda-13.0 /data/cuda-bak
(lyra2) root@ubuntu:/data/projects/lyra/Lyra-2# mv /usr/local/cuda-12.9 /data/cuda-bak2
